##
# Frontend / SSR app (TanStack Start)
#
# This must be set at BUILD time (Vite embeds VITE_* values).
#
# You can get this value from:
# - `npx convex dev` (local)
# - Convex dashboard -> Deployment settings (prod)
##
VITE_CONVEX_URL=https://your-deployment.convex.cloud

